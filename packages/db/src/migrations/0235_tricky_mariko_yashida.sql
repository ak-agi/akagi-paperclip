CREATE INDEX "document_revisions_company_created_by_run_idx" ON "document_revisions" USING btree ("company_id","created_by_run_id") WHERE "document_revisions"."created_by_run_id" is not null;--> statement-breakpoint
-- paperclip:migration-safety-ignore large-create-index-not-concurrently: Drizzle applies migrations transactionally, so CONCURRENTLY is unavailable. The predicate keeps the index to comments a heartbeat run actually wrote, and without it the orchestration cost read model sequentially scans every comment in the company once per cost-bearing run.
CREATE INDEX "issue_comments_company_created_by_run_idx" ON "issue_comments" USING btree ("company_id","created_by_run_id") WHERE "issue_comments"."created_by_run_id" is not null;--> statement-breakpoint
-- paperclip:migration-safety-ignore large-create-index-not-concurrently: Drizzle applies migrations transactionally, so CONCURRENTLY is unavailable. The predicate keeps the index to comments a heartbeat run actually wrote, and without it the orchestration cost read model sequentially scans every comment in the company once per cost-bearing run.
CREATE INDEX "issue_comments_company_derived_created_by_run_idx" ON "issue_comments" USING btree ("company_id","derived_created_by_run_id") WHERE "issue_comments"."derived_created_by_run_id" is not null;--> statement-breakpoint
CREATE INDEX "issue_work_products_company_created_by_run_idx" ON "issue_work_products" USING btree ("company_id","created_by_run_id") WHERE "issue_work_products"."created_by_run_id" is not null;